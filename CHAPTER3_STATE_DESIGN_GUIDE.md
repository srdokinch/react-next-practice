# 進め方の3章 手順書（状態設計）

この章は「ポモドーロタイマーに必要な状態（データ）を整理し、Reactの`useState`を使って状態管理の準備をする」ことがゴールです。  
まだタイマー機能は実装しません。まずは「どんなデータが必要か」を整理して、状態を定義します。

---

## ゴール
- 必要な状態が全て定義されている
- `useState`を使って状態を管理できるようになっている
- 状態から表示用の値を計算する方法が分かっている
- 状態の初期値が適切に設定されている

---

## 考え方：状態設計のアプローチ

### 1. 状態とは何か？
状態（state）は、コンポーネントが持つ「変わる可能性のあるデータ」です。
- 例：タイマーの残り秒数、ボタンが押されたかどうか、現在のモードなど
- 状態が変わると、画面が自動的に再描画されます

### 2. 必要な状態を洗い出す
ポモドーロタイマーで必要な状態を考えます：
- **残り秒数**：タイマーが何秒残っているか
- **実行中かどうか**：タイマーが動いているか止まっているか
- **現在のモード**：作業モードか休憩モードか
- **履歴**：完了したセッションの記録

### 3. 状態の関係を考える
状態同士がどう影響し合うかを考えます：
- 残り秒数が0になったら → 実行中フラグをfalseにする、履歴に追加する
- モードが変わったら → 残り秒数をそのモードの初期値にリセットする

---

## 手順

### ステップ1：useStateをインポートする

**やること：**
Reactから`useState`をインポートします。

**考え方：**
- `useState`はReactが提供するフック（Hook）の一つです
- フックは、関数コンポーネントで状態を管理するための機能です
- `useState`を使うと、コンポーネント内で状態を定義・更新できます

**ヒント：**
- ファイルの先頭（`"use client"`の後）にインポート文を追加します
- `import { useState } from "react";` という形式で書きます
- 複数のフックを使う場合は、`import { useState, useEffect } from "react";` のようにカンマで区切ります

**考え方の補足：**
- `useState`は「use」で始まる名前のフックです
- フックは関数コンポーネントのトップレベルで呼び出す必要があります（if文の中などでは使えません）

**確認ポイント：**
- `useState`が正しくインポートされているか
- エラーが出ていないか

---

### ステップ2：残り秒数の状態を定義する

**やること：**
タイマーの残り秒数を管理する状態を定義します。

**考え方：**
- タイマーは秒単位でカウントダウンします
- 表示は「分:秒」の形式ですが、内部では秒数で管理するのがシンプルです
- 例：25分 = 1500秒、5分 = 300秒

**ヒント：**
- `useState(初期値)` という形式で状態を定義します
- 例：`const [seconds, setSeconds] = useState(1500);`
- `seconds`が現在の値、`setSeconds`が値を更新する関数です
- 初期値は作業時間（25分 = 1500秒）にします

**考え方の補足：**
- なぜ秒数で管理するのか？
  - 計算が簡単（1秒ずつ減らすだけ）
  - 分と秒を別々に管理すると、秒が60になった時の処理が複雑になる
  - 秒数から分と秒を計算する方がシンプル
- 例：1500秒 → 25分0秒 → "25:00"という文字列に変換

**確認ポイント：**
- 状態が正しく定義されているか
- 初期値が適切か（1500秒 = 25分）

---

### ステップ3：実行中フラグの状態を定義する

**やること：**
タイマーが動いているかどうかを管理する状態を定義します。

**考え方：**
- タイマーは「動いている」か「止まっている」かの2つの状態があります
- これを真偽値（true/false）で管理します
- true = 実行中、false = 停止中

**ヒント：**
- `const [isRunning, setIsRunning] = useState(false);` という形式で定義します
- `isRunning`は「実行中かどうか」を表す変数名です
- 初期値は`false`（停止中）にします

**考え方の補足：**
- なぜこの状態が必要なのか？
  - ボタンの有効/無効を制御するため（実行中は「開始」ボタンを無効化）
  - タイマーが既に動いているかどうかを確認するため（重複起動を防ぐ）
  - UIの表示を変えるため（実行中は「停止」ボタンを表示など）

**確認ポイント：**
- 状態が正しく定義されているか
- 初期値が`false`になっているか

---

### ステップ4：モードの状態を定義する

**やること：**
現在のモード（作業/休憩）を管理する状態を定義します。

**考え方：**
- モードは「作業」か「休憩」かの2択です
- 文字列（"work" / "break"）または真偽値で管理できます
- 文字列の方が分かりやすいので、文字列を使うのがおすすめです

**ヒント：**
- `const [mode, setMode] = useState("work");` という形式で定義します
- `mode`は現在のモードを表す変数です
- 初期値は`"work"`（作業モード）にします
- 値は`"work"`（作業）と`"break"`（休憩）の2つを使います

**考え方の補足：**
- なぜ文字列を使うのか？
  - 分かりやすい（`mode === "work"` のように条件分岐が簡単）
  - 後で履歴に保存する時に、モード名がそのまま使える
- 他の方法もある？
  - 真偽値（`isWorkMode`）でも可能ですが、文字列の方が拡張しやすい

**確認ポイント：**
- 状態が正しく定義されているか
- 初期値が`"work"`になっているか

---

### ステップ5：作業時間・休憩時間の基準値を定義する

**やること：**
作業時間と休憩時間の基準値（初期値）を定義します。

**考え方：**
- 作業時間：通常25分（1500秒）
- 休憩時間：通常5分（300秒）
- これらは定数として定義します（状態ではなく、変更しない値）

**ヒント：**
- `const WORK_TIME = 1500;` と `const BREAK_TIME = 300;` という形式で定義します
- コンポーネントの外側（関数の外）で定義するのが一般的です
- 大文字で書くのは、定数（変更しない値）であることを示すためです

**考え方の補足：**
- なぜ状態ではなく定数なのか？
  - これらの値は変更しない（固定値）
  - 状態は「変わる可能性のあるデータ」なので、固定値は定数として定義する
- 後で変更できるようにする場合
  - もしユーザーが時間を設定できるようにするなら、状態にする必要があります
  - 今は固定値でOKです

**確認ポイント：**
- 定数が正しく定義されているか
- 値が正しいか（1500秒 = 25分、300秒 = 5分）

---

### ステップ6：履歴の状態を定義する

**やること：**
完了したセッションの履歴を管理する状態を定義します。

**考え方：**
- 履歴は複数のセッションを保存するので、配列で管理します
- 各セッションには、完了時刻やモードなどの情報が入ります
- 最初は空の配列から始めます

**ヒント：**
- `const [history, setHistory] = useState([]);` という形式で定義します
- `history`は配列で、各要素は履歴1件を表すオブジェクトです
- 初期値は空の配列`[]`にします

**考え方の補足：**
- 履歴1件にどんな情報を入れるか？
  - 完了時刻（日時）
  - モード（作業/休憩）
  - その他、必要に応じて追加
- 例：`{ completedAt: "2024-01-01 10:00", mode: "work" }`
- 後で履歴を追加する時は、`setHistory([...history, newItem])` のように配列に追加します

**確認ポイント：**
- 状態が正しく定義されているか
- 初期値が空の配列`[]`になっているか

---

### ステップ7：setIntervalの参照を定義する（オプション）

**やること：**
タイマーの`setInterval`の参照を保存する状態を定義します。

**考え方：**
- `setInterval`は、一定間隔で処理を実行する関数です
- タイマーを止める時は、`clearInterval`を使います
- `clearInterval`を使うには、`setInterval`の戻り値（ID）が必要です
- このIDを保存しておく必要があります

**ヒント：**
- `const [intervalId, setIntervalId] = useState(null);` という形式で定義します
- `intervalId`は`setInterval`の戻り値（ID）を保存します
- 初期値は`null`（まだタイマーが動いていない）にします

**考え方の補足：**
- なぜこの状態が必要なのか？
  - タイマーを止める時に`clearInterval(intervalId)`を使うため
  - タイマーが既に動いているかどうかを確認するため（重複起動を防ぐ）
- 後で実装する時に：
  - タイマーを開始する時：`const id = setInterval(...); setIntervalId(id);`
  - タイマーを止める時：`clearInterval(intervalId); setIntervalId(null);`

**確認ポイント：**
- 状態が正しく定義されているか
- 初期値が`null`になっているか

---

### ステップ8：表示用の分・秒を計算する関数を作る

**やること：**
残り秒数から、表示用の「分:秒」の文字列を計算する関数を作ります。

**考え方：**
- 残り秒数（例：1500秒）から、分と秒を計算します
- 分 = 秒数 ÷ 60（整数部分）
- 秒 = 秒数 % 60（余り）
- それを「分:秒」の形式の文字列にします

**ヒント：**
- 関数はコンポーネント内で定義します
- 例：
  ```javascript
  const formatTime = (totalSeconds) => {
    const minutes = Math.floor(totalSeconds / 60);
    const seconds = totalSeconds % 60;
    return `${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;
  };
  ```
- `padStart(2, '0')`は、数字が1桁の時に0を前に付けて2桁にする関数です（例：5 → "05"）

**考え方の補足：**
- なぜ関数にするのか？
  - 計算ロジックを1箇所にまとめられる
  - 後で表示する時に`formatTime(seconds)`のように簡単に使える
  - コードが読みやすくなる
- `Math.floor`とは？
  - 小数点以下を切り捨てる関数です
  - 例：`Math.floor(25.5)` → 25

**確認ポイント：**
- 関数が正しく定義されているか
  - 1500秒 → "25:00" になるか
  - 65秒 → "01:05" になるか
  - 5秒 → "00:05" になるか

---

## よくあるつまずき

- **`useState is not defined`というエラーが出る**  
  → `import { useState } from "react";` が正しく書かれているか確認してください。

- **状態が更新されない**  
  → `setSeconds(新しい値)` のように、更新関数を使っているか確認してください。直接`seconds = 新しい値`のように代入はできません。

- **状態の初期値が反映されない**  
  → `useState(初期値)` の形式が正しいか確認してください。

- **分・秒の計算が合わない**  
  → `Math.floor`を使っているか、`padStart`で0埋めしているか確認してください。

---

## 次に進む前のチェック

- [ ] `useState`がインポートされている
- [ ] 残り秒数の状態が定義されている（初期値：1500）
- [ ] 実行中フラグの状態が定義されている（初期値：false）
- [ ] モードの状態が定義されている（初期値："work"）
- [ ] 作業時間・休憩時間の基準値が定義されている（1500秒、300秒）
- [ ] 履歴の状態が定義されている（初期値：[]）
- [ ] `setInterval`の参照が定義されている（初期値：null）
- [ ] 表示用の分・秒を計算する関数が作られている

ここまでできたら、次章（タイマー機能実装）に進めます。

---

## 補足：状態の命名規則

状態の変数名は、分かりやすい名前を付けるのが重要です：

- **残り秒数**：`seconds`、`remainingSeconds`、`timeLeft`
- **実行中フラグ**：`isRunning`、`isActive`、`isTimerRunning`
- **モード**：`mode`、`currentMode`、`timerMode`
- **履歴**：`history`、`completedSessions`、`sessionHistory`

`is`で始まる変数名は真偽値（true/false）であることが分かりやすいのでおすすめです。

